# 不格式化：第三方/产物/资源
#
# ⚠️ 写法约定：凡是只想匹配**仓库根目录下**的条目，目录名必须带前导斜杠 `/xxx/`。
#
# gitignore 语义下，不带前导斜杠的 `assets/` 会匹配**任意层级**的同名目录。
# 曾经 `assets/` 就顺带排除了 `ui/assets/`（整个前端脚本目录），导致 Prettier
# **静默跳过全部前端文件**——`--check` 依旧输出 "All matched files use Prettier
# code style!"，看起来是通过了，实际一个文件都没检查。
# 排查方法：往目标目录放一个故意格式错误的文件，看 `--check` 是否会报出来。

/node_modules/
/dist/
/assets/
/images/
/pnpm-lock.yaml

# scripts/*.mjs 是构建脚本，沿用原有单引号风格、由人工维护；
# 交给 prettier 会整篇改写引号与换行，产生大量与功能无关的 diff 噪音。
# （仍受 ESLint 检查，只是不参与自动格式化。）
/scripts/

# Cargo 生成的锁文件（由 cargo 维护，不交给 prettier）
/src-tauri/Cargo.lock

# 构建产物与外部资源（这些目录名在子层级不存在同名目录，无需担心误伤）
src-tauri/target/
src-tauri/gen/
src-tauri/resources/
src-tauri/icons/

# 大段内联 SVG 的 HTML 交给人工维护（prettier 会重排 SVG path，diff 噪音大）
ui/index.html
